<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.setditjenp2mkt.apputs"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAAVZI__INKqkTYV0apCylY2Fi1aEdadlU" />

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".KotaActivity"
            android:parentActivityName=".MainActivity"></activity>
        <!-- Parent activity meta-data to support 4.0 and lower -->


        <activity android:name=".WisataActivity"
            android:parentActivityName=".KotaActivity"></activity>
        <!-- Parent activity meta-data to support 4.0 and lower -->


        <activity android:name=".KulinerActivity"
            android:parentActivityName=".KotaActivity"></activity>
        <!-- Parent activity meta-data to support 4.0 and lower -->


        <activity android:name=".AddActivity"
            android:parentActivityName=".MainActivity"></activity>
        <!-- Parent activity meta-data to support 4.0 and lower -->


        <activity android:name=".EditActivity"
            android:parentActivityName=".KotaActivity"></activity>

    </application>

</manifest>